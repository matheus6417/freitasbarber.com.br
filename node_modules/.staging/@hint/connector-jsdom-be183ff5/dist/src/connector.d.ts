/// <reference types="node" />
import { URL } from 'url';
import { HttpHeaders, IConnector, NetworkData } from 'hint/dist/src/lib/types';
import { JSDOMAsyncHTMLElement, JSDOMAsyncHTMLDocument } from 'hint/dist/src/lib/types/jsdom-async-html';
import { Engine } from 'hint/dist/src/lib/engine';
import { Requester } from '@hint/utils-connector-tools/dist/src/requester';
export default class JSDOMConnector implements IConnector {
    private _options;
    private _href;
    private _targetNetworkData;
    private _window;
    private _document;
    private _timeout;
    private _resourceLoader;
    request: Requester;
    server: Engine;
    finalHref: string;
    fetchedHrefs: Set<string>;
    constructor(server: Engine, config?: object);
    private _fetchUrl;
    private traverseAndNotify;
    private getFavicon;
    collect(target: URL): Promise<{}>;
    close(): Promise<void>;
    fetchContent(target: URL | string, customHeaders?: object): Promise<NetworkData>;
    private killProcess;
    evaluate(source: string): Promise<any>;
    querySelectorAll(selector: string): Promise<JSDOMAsyncHTMLElement[]>;
    readonly dom: JSDOMAsyncHTMLDocument;
    readonly headers: HttpHeaders;
    readonly html: Promise<string>;
}
