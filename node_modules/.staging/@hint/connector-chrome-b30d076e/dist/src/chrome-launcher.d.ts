import { Launcher } from '@hint/utils-debugging-protocol-common/dist/src/launcher';
import { BrowserInfo, LauncherOptions } from 'hint/dist/src/lib/types';
export declare class CDPLauncher extends Launcher {
    private userDataDir;
    private chromeFlags;
    constructor(options: LauncherOptions);
    private getBrowserInfo;
    private writePid;
    launch(url: string): Promise<BrowserInfo>;
}
